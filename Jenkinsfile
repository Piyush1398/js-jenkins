pipeline {
    agent any
    stages {
        stage('verify version') {
            steps {
                echo "hello"
                echo "hey"
            }
        }
        stage('build') {
        steps{

        sh 'NodeJS app.js'
        }
       }
    }
}
